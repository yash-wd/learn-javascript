{
  "name": "learn-javascript",
  "version": "1.0.0",
  "description": "A complete step-by-step JavaScript course — 52 lessons + 6 projects, read it then run it.",
  "license": "MIT",
  "private": true,
  "_comment": "No \"type\" field on purpose: .js stays CommonJS by default (lesson 22 relies on this), while Node still auto-detects the few lessons that use ESM import syntax (37, 38, 45). Setting type to commonjs OR module would break one group or the other.",
  "scripts": {
    "lesson": "node",
    "test": "node --test lessons/38-testing.js",
    "verify": "bash scripts/verify.sh",
    "bundle": "node tools/build-bundle.mjs"
  },
  "keywords": ["javascript", "course", "tutorial", "learning", "beginner"],
  "engines": {
    "node": ">=20"
  }
}
