# Dependencies
node_modules/
.pnp/
.pnp.js

# Logs & caches
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.cache/
.parcel-cache/

# Build output (not used here, but future-proofed)
dist/
build/

# Generated bundle intermediate (the .md and .pdf are committed; the .html is
# only a transient step to produce the PDF, so it's regenerated on demand).
bundle/JS_Learn_Everything.html

# Environment & secrets
.env
.env.*
!.env.example

# Cloudflare Wrangler (deploy tooling) — local state, never committed
.wrangler/
.dev.vars

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS cruft
.DS_Store
Thumbs.db
desktop.ini
