/* PRACTICE · 06 Numbers & Math ─────────────────────────────────────────────
 * Fill in the bodies, then grade your work:
 *   PRACTICE=mine node --test practice/06-numbers.test.mjs
 * Peek at 06-numbers.solution.mjs only after you've tried.
 * ------------------------------------------------------------------------- */

// clamp(n, min, max): return n limited to the range [min, max].
//   clamp(5, 0, 10) === 5 · clamp(-3, 0, 10) === 0 · clamp(99, 0, 10) === 10
export function clamp(n, min, max) {
  // TODO
}

// isPrime(n): true if n is a prime (n >= 2 with no divisor other than 1 and n).
//   isPrime(2) true · isPrime(9) false · isPrime(13) true · isPrime(1) false
export function isPrime(n) {
  // TODO
}

// roundTo(n, dp): round n to dp decimal places.
//   roundTo(1.2345, 2) === 1.23 · roundTo(2.5, 0) === 3
export function roundTo(n, dp) {
  // TODO
}
