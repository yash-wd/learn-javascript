/* PRACTICE · 13 Array Methods ───────────────────────────────────────────────
 * Fill in the bodies, then grade your work:
 *   PRACTICE=mine node --test practice/13-array-methods.test.mjs
 * Aim to use map/filter/reduce rather than manual loops.
 * ------------------------------------------------------------------------- */

// sumOfEvens(nums): add up only the even numbers.  sumOfEvens([1,2,3,4]) === 6
export function sumOfEvens(nums) {
  // TODO
}

// groupBy(arr, keyFn): bucket items into an object keyed by keyFn(item).
//   groupBy([1,2,3,4], n => n % 2 ? 'odd' : 'even') === { odd:[1,3], even:[2,4] }
export function groupBy(arr, keyFn) {
  // TODO
}

// countWords(str): count occurrences of each whitespace-separated word.
//   countWords('the cat the dog') === { the:2, cat:1, dog:1 }
export function countWords(str) {
  // TODO
}
